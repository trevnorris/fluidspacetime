{
  "name": "fluid-spacetime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:client": "vite build",
    "build:server": "vite build --ssr src/entry-server.jsx --outDir .server-build",
    "prerender": "node scripts/prerender.js",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir .server-build && node scripts/prerender.js",
    "preview": "vite preview --outDir dist"
  },
  "dependencies": {
    "katex": "^0.16.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.27.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^5.4.10"
  }
}
